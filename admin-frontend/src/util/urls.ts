const rootUrl = "http://localhost:3000"

// Register new user
export const createUserEndpoint = rootUrl + "/user"
export const getUserEndpoint = rootUrl + "/user"
export const loginEndpoint = rootUrl + "/auth/login"